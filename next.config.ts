import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is the default in Next.js 16
  // No special configuration needed for bwip-js or qrcode
};

export default nextConfig;
